# Environment variables for production deployment
# These can be set in your Render dashboard

# Server configuration
PORT=8080
HOST=0.0.0.0

# Application settings
ENVIRONMENT=production
DEBUG=False

# CORS settings (update with your domain after deployment)
CORS_ORIGINS=["*"]

# Logging
LOG_LEVEL=info
